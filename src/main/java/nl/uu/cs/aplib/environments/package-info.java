/**
 * Providing several implementations of {@link nl.uu.cs.aplib.mainConcepts.Environment},
 * e.g. {@link ConsoleEnvironment} that can read and write to the console, and
 * {@link NullEnvironment} that does not do anything (but still consumes commands
 * sent to it).
 */
package nl.uu.cs.aplib.environments;